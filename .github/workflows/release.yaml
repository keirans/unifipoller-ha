name: Release

on:
  release:
    types:
      - published

jobs:
  release:
    name: Build and Push Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse release tag
        id: tag
        run: |
          # Strip leading 'v' if present: v2.9.0-1 -> 2.9.0-1
          TAG="${{ github.event.release.tag_name }}"
          VERSION="${TAG#v}"
          UNPOLLER_VERSION=$(echo "$VERSION" | cut -d'-' -f1)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "unpoller_version=$UNPOLLER_VERSION" >> "$GITHUB_OUTPUT"

      - name: Build and push release image
        uses: docker/build-push-action@v5
        with:
          context: unifi-poller
          push: true
          tags: |
            ghcr.io/keirans/ha-addon-unifi-poller:amd64-${{ steps.tag.outputs.version }}
          build-args: |
            UNPOLLER_VERSION=${{ steps.tag.outputs.unpoller_version }}
